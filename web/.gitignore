node_modules/
dist/
.vite/
*.local
*.tsbuildinfo

# OpenAPI codegen output — regenerated by `npm run gen:api`. Keep .gitkeep
# tracked so the directory survives a fresh checkout.
src/api/generated/*
!src/api/generated/.gitkeep
